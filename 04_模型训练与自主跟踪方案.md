# 模型训练与自主跟踪方案

## 四、基于视觉基座与时序预测的快慢双环跟踪

### 1. 总体方案

采用“快速检测与控制＋低频基座校正”的艇载架构。YOLO与ByteTrack持续跟踪，DINOv3定期校正目标关联，传统图像伺服和飞控执行运动控制。后续增加时序预测，辅助转向和减速。

所有在线模型运行于Orin。任务期间模型权重固定，每十帧进行推理校正，不执行梯度训练；微调在岸端完成。

```text
图像 → YOLO11n → ByteTrack → 目标状态 → 图像伺服 → 飞控
 │ ↑
 └─ 每十个处理帧／事件触发 → DINOv3＋目标记忆
 ↓
关联、漂移检查与重捕校正
后续：视频特征＋艇体状态＋历史动作 → 时序预测 → 受限策略修正
```

### 2. 模型与运行频率

| 模块 | 模型或方法 | 运行方式 |
|---|---|---|
| 快速检测 | YOLO11n预训练权重，公开鱼类数据微调 | 目标10-20Hz |
| 短时跟踪 | ByteTrack | 随检测更新 |
| 低频校正 | DINOv3 ViT-S/16，约21M参数，冻结骨干 | 每十个处理帧触发，约1-2Hz请求频率 |
| 跟随控制 | 图像伺服PD/PID | 10Hz发布受限控制建议 |
| 底层执行 | ArduSub姿态、深度与混控 | 飞控实时运行 |
| 后续预测 | 岸端V-JEPA 2.1教师，艇端轻量时序模型 | 低频异步执行 |

频率为设计目标，以全链路同时运行结果验收。只处理有限数量候选区域，限制模板数量和输入尺寸；低频调用仍需满足Orin内存与单次推理开销要求。

### 3. 快速跟踪与传统控制

YOLO输出鱼框和置信度，ByteTrack关联连续帧。单鱼模式使用连续可见性和切换滞回；鱼群模式跟随有效鱼框的群体中心。

目标框中心为 `(u,v)`，图像大小为 `(W,H)`：

```text
ex = (u - W/2) / (W/2)
ey = (v - H/2) / (H/2)
```

| 条件 | 控制动作 |
|---|---|
| 目标偏离中心 | 减小前进，优先转向 |
| 目标稳定居中 | 允许低输出前进 |
| 鱼框快速增大 | 减速或停止接近 |
| 目标丢失 | 取消追近，限时转向搜索，超时退出 |
| 感知过期或设备异常 | 撤销跟随，执行底层退出策略 |

首轮固定深度，稳定后增加缓慢深度调整。控制加入死区、限幅和变化率限制。框面积不换算米制鱼距；朝下声纳只提供离底约束。所有修正通过ROS 2仲裁后执行。

### 4. DINOv3低频校正

#### 4.1 目标记忆与关联

目标连续稳定可见时，提取鱼体区域特征，建立少量高置信度外观模板。低频链路对当前候选提取特征，结合外观相似度、预测位置和运动连续性判断关联，不单独依靠特征相似度切换目标。

| 场景 | 校正内容 |
|---|---|
| 正常跟随 | 检查当前目标与模板是否一致 |
| 两鱼交叉 | 对比候选外观与运动，修正关联 |
| 跟踪漂移 | 降低目标置信度，停止错误追近 |
| 目标重现 | 对候选排序，重新确认后恢复跟随 |

模板只在高置信度一致匹配时更新。遮挡、低置信度和候选冲突期间冻结模板，避免将背景或其他鱼写入记忆。

外观相似的鱼仍可能混淆，短时轨迹不作为永久个体身份。

#### 4.2 异步调度

- 默认每十个已处理图像帧触发一次；20Hz快速链路对应约2Hz请求。
- 置信度下降、鱼体交叉、目标接近视场边缘时提前触发。
- 同时最多执行一次校正，等待队列仅保留最新请求，事件触发同样限流。
- 快速链路不等待校正结果；结果携带原图时间、帧号、目标代次和置信度。
- 校正门控检查数据年龄与当前目标一致性；过旧或目标已切换的结果直接丢弃。
- 需要位置修正时先映射到当前帧，再平滑应用，不用旧目标框覆盖当前观测。

慢链路超时或资源超限时暂停校正；快速链路健康且目标可信时继续基线跟踪，否则退出追近。慢链路不能绕过快速状态估计直接控制推进器。

### 5. 公开数据与微调

| 对象 | 训练方案 | 数据来源 |
|---|---|---|
| YOLO11n | 加载 `yolo11n.pt`，先适配鱼类检测头，再低学习率微调后部网络 | Brackish等已有目标框标签的数据 |
| DINOv3 | 首版冻结权重，使用特征匹配；后续按需训练小型匹配头 | 公开数据及本机高置信度片段 |
| 本机适配 | 仅在回放显示明显域差异时继续微调 | 教师候选标签，首轮人工复核约50-100帧 |
| 独立验收 | 不参与离线训练与阈值选择，运行模板仅由在线观测建立 | 独立航次及人工核验片段 |

YOLO11n通用COCO类别不包含鱼，需使用公开鱼类检测标签适配。Grounding DINO作为岸端候选框教师；SAM 3/3.1可用于离线分割和跟踪对照，不加入艇载必需链路。

训练以现有标签为主，不设置数千张人工标注任务。教师标签需抽查，漏标帧不直接当作无鱼负样本。按源视频和航次隔离训练、验证、测试，教师生成数据随源片段归组。

输入尺寸以640为YOLO训练起点，使用验证集早停。模型完成微调后固定权重部署；现场只更新目标模板和状态，不修改网络参数。

### 6. 时序预测与具身扩展

在双环基线稳定后，增加艇体状态和动作反馈，预测目标是否将离开视野。

| 项目 | 内容 |
|---|---|
| 输入 | 最近视频特征、目标轨迹、姿态、角速度、深度和历史控制 |
| 输出 | 短时目标图像位置、预测置信度、转向修正和减速建议 |
| 岸端训练 | V-JEPA 2.1提取时序教师特征，训练或蒸馏轻量时序头 |
| 艇端部署 | 使用小型时序模型异步推理，不直接加载大型教师 |
| 对比基线 | 恒速预测及无预测的DINOv3双环 |

数据由艇载记录形成，高置信度轨迹用于构建候选监督，人工重点检查丢失、重捕和验证片段。输入仅使用当前及历史观测，未来可见轨迹用于训练目标；不使用未来信息构造部署输入。

首先学习当前策略下的短时运动预测。引入不同动作的效果预测时，另行采集覆盖相应动作的轨迹。预测只提供受限修正，目标丢失时仍禁止盲目前进。

首版交付为检测、跟踪、DINOv3校正和传统控制。时序预测为第二阶段研究内容，通用VLA不作为交付前置条件。

### 7. Orin部署与实验

检测器采用ONNX → TensorRT FP16部署；DINOv3冻结推理，验证算子和导出精度后构建端侧引擎。INT8仅在代表性数据校准并通过精度检查后采用。

快速检测、低频校正、控制和记录分进程运行，共享GPU负载统一监测。低频任务不积压，超限时降低其请求频率或候选数量，保留快速闭环资源。

| 实验组 | 配置 | 主要指标 |
|---|---|---|
| A | YOLO＋ByteTrack＋图像伺服 | 基线成功率、误切目标、丢失 |
| B | A＋每十帧DINOv3校正 | 关联改善、重捕时间、计算开销 |
| C | B＋事件触发 | 关键时刻响应与额外推理次数 |
| D | C＋恒速预测 | 预测辅助控制基线 |
| E | C＋学习的时序预测 | 相对D组的跟随增益 |

各组使用固定数据划分、相同试验条件及独立评价；记录错误校正、模板污染、过期丢弃和慢链路故障，验证闭环慢链路后能回退基线。

鱼形靶、真鱼分级验收，单鱼与鱼群分别统计。每级每种模式至少10次独立尝试，每次30-60秒，成功目标至少8/10。

成功要求：

- 无人工接管或异常退出；
- 目标可见时间占计划时长至少80%；
- 可见时位于画面中央50%宽高区域的时间比例至少80%。

快速感知目标10-20Hz，采集至控制桥接输出P95不超过200ms，稳定段深度误差±0.15m。慢链路另报请求率、完成率、P95耗时及结果年龄，不将请求频率视为实际完成频率。指标均以整机实测验收。

### 8. 交付与资源

交付：基座及数据清单、微调权重、端侧引擎、模板与校正配置、异步调度参数、跟踪控制参数、消融及水试报告。

- YOLO11
- DINOv3
- V-JEPA 2.1
- Grounding DINO
- SAM 3/3.1
- Brackish数据集
